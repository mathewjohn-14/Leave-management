apiVersion: v1
kind: Service
metadata:
  name: leave-management-service
spec:
  type: NodePort
  selector:
    app: leave-management
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007